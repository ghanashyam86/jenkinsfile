pipeline{
    agent {
         label "built-in"
         }
      stages {
          stage ('hello-world') {
              steps {
                   echo " this is index.html file and create in jenkinsfile"
                   
                   }
                 }
               }
               
             }
